---
phase: 05-rest-api-web-ui-production-dockerfile
plan: 09
type: execute
wave: 5
depends_on: [05-05, 05-06, 05-03]
files_modified:
  - web/src/pages/admin/UsersPage.tsx
  - web/src/pages/admin/AuditPage.tsx
  - web/src/pages/admin/TlsPage.tsx
  - web/src/pages/admin/TrivyDbPage.tsx
  - web/src/pages/admin/GcPage.tsx
  - web/src/pages/admin/TrashPage.tsx
  - web/src/pages/admin/MaintenancePage.tsx
autonomous: true
requirements:
  - UI-11

must_haves:
  truths:
    - "Users page shows CRUD table with create/edit/delete modals"
    - "Audit page shows filterable table with detail drawer"
    - "TLS page shows current cert info + upload + history"
    - "Trivy DB page shows status card + upload/pull actions"
    - "GC page shows trigger button + last run stats"
    - "Trash page shows table with restore/purge + bulk actions"
    - "Maintenance page shows toggle switch with confirmation"
  artifacts:
    - path: "web/src/pages/admin/UsersPage.tsx"
      provides: "User CRUD per D-22"
      contains: "Create User"
    - path: "web/src/pages/admin/AuditPage.tsx"
      provides: "Audit log per D-19"
      contains: "Audit Log"
    - path: "web/src/pages/admin/TrivyDbPage.tsx"
      provides: "Trivy DB admin per D-20"
      contains: "Trivy Database"
  key_links:
    - from: "web/src/pages/admin/UsersPage.tsx"
      to: "/api/v1/admin/users"
      via: "TanStack Query"
      pattern: "admin/users"
---

<objective>
Build all 7 admin pages: Users, Audit Log, TLS Certs, Trivy DB, GC, Trash, and Maintenance. These are lazy-loaded and super-admin-only.

Purpose: Admin pages are the super-admin's control panel for the entire OmniRepo instance.
Output: 7 admin page components matching D-19 through D-25.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rest-api-web-ui-production-dockerfile/05-CONTEXT.md
@.planning/phases/05-rest-api-web-ui-production-dockerfile/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Users + Audit + TLS + Trivy DB admin pages</name>
  <files>web/src/pages/admin/UsersPage.tsx, web/src/pages/admin/AuditPage.tsx, web/src/pages/admin/TlsPage.tsx, web/src/pages/admin/TrivyDbPage.tsx</files>
  <read_first>
    - web/src/api/types.ts
    - web/src/api/queries.ts
    - web/src/components/common/DataTable.tsx
    - web/src/components/common/OneTimeReveal.tsx
    - web/src/components/common/FilterChips.tsx
    - web/src/components/common/Dropzone.tsx
  </read_first>
  <action>
**1. `web/src/pages/admin/UsersPage.tsx` (D-22):**

- Page title: "Users"
- DataTable: Avatar (dicebear from seed), Login, Email, Role (super-admin badge), Projects (tag chips), Created, Last Login
- "Create User" button opens Dialog:
  - Login field, Email field
  - Project assignments (multi-select checkboxes using existing projects list)
  - On submit: POST /api/v1/admin/users
  - Dialog transitions to OneTimeReveal showing generated one-time password
  - Warning: "This password will not be shown again. Copy it now and share it securely with the user."
- Edit user: click row opens Dialog with editable fields:
  - Email, is_super_admin toggle, project assignments
  - Force password reset toggle (sets must_change_password=true)
- Delete user: confirmation Dialog per 05-UI-SPEC destructive pattern
- Paginated via cursor

**2. `web/src/pages/admin/AuditPage.tsx` (D-19):**

- Page title: "Audit Log"
- Full-width DataTable: Timestamp, Actor (avatar + name), Action, Target, Outcome (success/fail Badge), IP
- Filter bar with dropdowns per D-19:
  - Actor (Select from user list)
  - Action type (Select from event kinds)
  - Target kind (Select: user/project/repo/etc.)
  - Date range picker (two date inputs)
  - Outcome (Select: success/failure)
- Click row: slide-out Sheet on right showing full `details_json` in formatted JSON (monospace, pretty-printed) in ScrollArea
- Cursor-based pagination with load more button
- Export button: downloads CSV or JSON (client-side generation from loaded data, or server endpoint)

**3. `web/src/pages/admin/TlsPage.tsx` (D-23):**

- Page title: "TLS Certificates"
- Current cert card: Subject, Issuer, Expiry Date, Fingerprint (from GET /api/v1/admin/tls/current)
- Upload form: two file inputs (cert PEM, key PEM) or paste areas
- "Upload Certificate" CTA button
- On success: toast "Certificate uploaded and hot-swapped successfully."
- History table: list from GET /api/v1/admin/tls/history (timestamp, subject, expiry)

**4. `web/src/pages/admin/TrivyDbPage.tsx` (D-20):**

- Page title: "Trivy Database"
- Status card at top: Version, Age (human-readable via formatDate), Source (Badge: baked-in/uploaded/online-pulled)
- Warning banner (orange) when DB older than threshold: "Trivy database is {age} old. Consider updating for the latest vulnerability data."
- Two action cards side by side:
  - "Upload DB": Dropzone for tarball, progress bar. POST /api/v1/admin/trivy/db
  - "Pull from Internet": Button with spinner during pull. POST /api/v1/admin/trivy/db/pull
    - On network error: toast "Unable to reach the Trivy database server. This is expected in air-gapped environments. Upload a DB tarball instead."
- History table: list of past DB updates (date, source, size) from API
  </action>
  <verify>
    <automated>cd /home/vladoportos/omnirepo/web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/admin/UsersPage.tsx contains "Create User" button
    - web/src/pages/admin/UsersPage.tsx uses OneTimeReveal for password display
    - web/src/pages/admin/AuditPage.tsx has filter dropdowns for Actor, Action, Target, Date range, Outcome
    - web/src/pages/admin/AuditPage.tsx uses Sheet for detail drawer
    - web/src/pages/admin/TlsPage.tsx has file inputs for cert and key PEM
    - web/src/pages/admin/TrivyDbPage.tsx shows DB status card with version, age, source
    - web/src/pages/admin/TrivyDbPage.tsx has both Upload and Pull actions
    - npm run build succeeds
  </acceptance_criteria>
  <done>Users CRUD, audit log with filters, TLS cert admin, and Trivy DB admin pages all functional.</done>
</task>

<task type="auto">
  <name>Task 2: GC + Trash + Maintenance admin pages</name>
  <files>web/src/pages/admin/GcPage.tsx, web/src/pages/admin/TrashPage.tsx, web/src/pages/admin/MaintenancePage.tsx</files>
  <read_first>
    - web/src/api/types.ts
    - web/src/api/queries.ts
    - web/src/components/common/DataTable.tsx
  </read_first>
  <action>
**1. `web/src/pages/admin/GcPage.tsx` (D-24):**

- Page title: "Garbage Collection"
- "Run Garbage Collection" button (destructive variant)
- Confirmation Dialog: "Run GC: This will permanently delete orphan blobs and expired trash entries. Continue?"
- While running: spinner + "Garbage collection in progress..." text
- Last GC run stats card: blobs deleted, bytes freed, trash entries deleted, duration
- Uses POST /api/v1/admin/gc and GET /api/v1/admin/gc/status

**2. `web/src/pages/admin/TrashPage.tsx` (D-21):**

- Page title: "Trash"
- Header: retention window config, total trash size
- Empty state: "Trash is empty" / "Deleted items will appear here..." per 05-UI-SPEC
- DataTable: Checkbox, Name, Type (repo/file Badge), Original Location, Deleted By, Deleted At, Retention Countdown (human-readable "5d left")
- Per-row actions: Restore button, Purge button (destructive)
- Purge confirmation: "Purge Item: This will permanently delete this item. This cannot be undone. Continue?"
- Bulk actions toolbar (appears when checkboxes selected): Restore Selected, Purge Selected
- Paginated via cursor

**3. `web/src/pages/admin/MaintenancePage.tsx` (D-25):**

- Page title: "Maintenance"
- Toggle Switch (shadcn Switch) with label "Maintenance Mode"
- Current status: who toggled and when
- Enable confirmation Dialog: "Enable Maintenance Mode: All write operations will return 503. Reads will continue to work. Continue?"
- Disable: immediate toggle (no confirmation)
- Status text showing current state
- Note: the global maintenance banner (MaintenanceBanner.tsx) shows on all pages when active
  </action>
  <verify>
    <automated>cd /home/vladoportos/omnirepo/web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/admin/GcPage.tsx has "Run Garbage Collection" button
    - web/src/pages/admin/GcPage.tsx shows last run stats
    - web/src/pages/admin/TrashPage.tsx has bulk Restore/Purge actions
    - web/src/pages/admin/TrashPage.tsx shows retention countdown
    - web/src/pages/admin/MaintenancePage.tsx uses Switch component for toggle
    - web/src/pages/admin/MaintenancePage.tsx has confirmation dialog for enable
    - npm run build succeeds
  </acceptance_criteria>
  <done>GC trigger, trash viewer with restore/purge, and maintenance toggle pages all functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin pages -> admin API | Super-admin-only UI; relies on server auth gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Elevation | admin pages | mitigate | Admin pages hidden in sidebar for non-admins (UI); server returns 403 for non-super-admin requests (defense in depth) |
| T-05-09-02 | Tampering | trash purge | mitigate | Destructive actions require confirmation dialog with explicit user action; server validates super-admin auth |
</threat_model>

<verification>
- All 7 admin pages render
- Users page shows create modal with one-time password reveal
- Audit page filters work
- Maintenance toggle persists
</verification>

<success_criteria>
- UI-11: All admin screens functional
- D-19 through D-25: Each admin page matches its decision spec
</success_criteria>

<output>
After completion, create `.planning/phases/05-rest-api-web-ui-production-dockerfile/05-09-SUMMARY.md`
</output>
